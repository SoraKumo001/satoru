# Deno OGP Service

High-speed OGP image generation using Satoru on Deno.

## Preparation

Ensure the core library is built:

```bash
pnpm build
```

## Usage

```bash
deno task dev
```

Then visit `http://localhost:8000/?title=Hello&subtitle=Deno`.
