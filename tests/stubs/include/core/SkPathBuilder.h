#pragma once
// Minimal SkPathBuilder stub for native tests (no full Skia dependency).
// The test stub SkPath.h provides a SkPathBuilder class — this header just
// provides the standalone include path used by container_skia_clip.cpp.

#include "SkPath.h"
